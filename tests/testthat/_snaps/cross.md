# dots must be unnamed

    Code
      fct_cross(x = f)
    Condition
      Error in `fct_cross()`:
      ! Arguments in `...` must be passed by position, not name.
      x Problematic argument:
      * x = f

